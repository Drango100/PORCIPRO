# Server
PORT=4000
FRONTEND_ORIGIN=http://localhost:5173
AUTO_MIGRATE=true
NODE_ENV=development

# MySQL (convención corta)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=authNode
DB_USER=root
DB_PASS=

# MySQL (convención Laravel) — alias para el linter y por si algún archivo usa estos
DB_CONNECTION=mysql
DB_DATABASE=authNode
DB_USERNAME=root
DB_PASSWORD=

# Auth
JWT_SECRET=pon-uno-bien-largo-y-unico
JWT_EXPIRES=7d
COOKIE_NAME=auth_token

BCRYPT_ROUNDS=10